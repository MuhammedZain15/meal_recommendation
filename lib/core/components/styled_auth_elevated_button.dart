import 'package:flutter/material.dart';

class StyledAuthElevatedButton extends StatelessWidget {
  const StyledAuthElevatedButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}